// 🧠 로컬 EXAONE Web Worker 구동 엔진 — Phase 2 (스텁).
// wllama / WASM / WebGPU로 브라우저 안에서 모델 실행 예정.

export async function runLocalEngine(_prompt: string): Promise<string> {
  throw new Error("Local engine not implemented yet (Phase 2)");
}
