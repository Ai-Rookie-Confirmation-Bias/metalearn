"""커리큘럼 에이전트들.

**한 프롬프트에 "커리큘럼 만들어줘 + 인출 만들어줘 + 형성평가 만들어줘"를 붙이지
않는다**(yoonhs 지시). 역할마다 나눈다. 지시가 서로 밀어내기 때문이다 —
실측에서 빈칸 유형을 자세히 쓸수록 설명 규칙이 묻혔고, 결국 문항 63%가
정의문 되읽기가 됐다.

에이전트마다 갖는 것:
    입력 계약 · 자기 프롬프트 · 자기 응답 검증 · **자기 품질 지표**

지표를 자기가 갖는 게 중요하다. 시키는 쪽과 재는 쪽이 같으면 자기 채점이 되므로,
판정은 별도 순수 로직(`retrieval_level`)이 하고 에이전트는 그걸 **호출**한다.

    explanation  절 → 설명 + 비유          (읽을 것)
    retrieval    절 + 설명 → 빈칸 + 객관식  (꺼낼 것)
    ※ 형성평가·절 재묶기는 나중에 여기 붙는다
"""
