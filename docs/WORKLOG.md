# 문제 생성 기능 — 세션별 작업 일지

> **새 세션은 이 파일부터 읽으세요.** 어느 세션(웹/로컬 CLI/데스크톱)에서 무엇을 했고,
> 지금 어디까지 왔고, 다음이 무엇인지가 전부 여기 있습니다.
> 문서 지도: 설계 [QUIZ.md](QUIZ.md) · 입력 계약 [QUIZ_INPUT.md](QUIZ_INPUT.md) · 품질 튜닝 상세 [QUIZ_TUNING.md](QUIZ_TUNING.md) · 전체 제품 설계 [../README.md](../README.md)

브랜치: `feat/quiz-create` · 실행: `cd backend && uv run pytest` / 스모크 `uv run python scripts/smoke_quiz.py --chunks 3 --budget 20`

---

## 세션 1 — 설계 + 백엔드 전체 구현 (08-05 새벽, 웹 세션)

**한 일:** 확정안(08-03)과 팀원 파싱 결과(md 예시 2개)를 분석해 파이프라인을 설계하고 백엔드를 전부 구현.

| 커밋 | 내용 |
| --- | --- |
| `f86c6fc` | 설계 문서 docs/QUIZ.md — 8단계 파이프라인(수신→선별→기출프로파일→계획→생성→검증→저장→서빙), 규칙 5개(파싱직후 생성·개인화 없음·데이터 격리·근거 필수·기출은 스타일만) |
| `e1faa09` | `backend/app/features/quiz/` 전체 구현 + 테스트 36개. 내부 모델 `ParsedDocument`(입력 형식 md/JSON과 격리), quiz_items/quiz_attempts 마이그레이션(0002, 학습 attempts와 물리 분리), API 4개(생성 트리거/목차 요약/세션 샘플링/채점). 실제 파싱 md에서 추출한 픽스처(`tests/fixtures/parsed_sample.json`)로 테스트 |
| `26ad8a1` | 파서 버전 게이트를 기본 무제한으로 (설정 시에만 잠금) |
| `79910d0` | docs/QUIZ_INPUT.md — 팀원 파서용 JSON 입력 계약 잠정안 (팀원이 "JSON으로 준다" 확정, 형식 상세는 미확정) |
| `a6e9f8f` | `scripts/smoke_quiz.py` — DB·도커 없이 Solar 실호출로 문항 품질 확인하는 스크립트 |

핵심 결정: 근거는 LLM이 문장 번호(sN)로 답하고 코드가 offset·원문으로 변환 / 예산 = clamp(개념수×1.5, 15~80) + 개념당 1~3 / 유형은 원문 형태(정의·열거·순서·대비)가 결정.

## 세션 2 — 프롬프트 튜닝 1~6회차 (08-05 오후, 로컬 CLI·Opus)

**한 일:** Solar 실호출 6회로 문항 품질 점검, 프롬프트 규칙 4→12개·심판 3→11개 확장.

| 커밋 | 내용 |
| --- | --- |
| `c2b63ed` | 채점이 "공백 제거 후 완전일치"임을 프롬프트에 명시, 단답 정답 25자 이내 용어화, 빈칸 1~2개 제한, 특수문자 aliases 병기, 시험 문체 강제 |
| `78c9542` | docs/QUIZ_TUNING.md 작성 — 실행 기록·발견 문제·남은 작업(§5) 인수인계. **"폐기율이 아니라 저장분 중 실사용 가능 비율을 봐라"** 원칙 수립 |

발견: `.env`가 UTF-16이면 dotenv가 죽음(§0) / 남은 불량은 전부 cloze → 원인은 프롬프트가 아니라 코드(§5).

## 세션 3 — QUIZ_TUNING §5 코드 반영 (08-05, 웹 세션)

| 커밋 | 내용 |
| --- | --- |
| `9ea0376` | ① classify_form: "단계/순서" 키워드 제거, 화살표 2회 이상만 sequence + sequence 후보에 mcq 추가(cloze 강제 해소, 오분류 4건 전멸) ② 기계검사: mcq 정확히 4선지, cloze 정답 지문 노출 폐기, alias 중복 정리 ③ 생성 응답 파싱 실패 시 재시도 1회+리포트 명시(조용한 실패 제거). 테스트 42개 |

7회차 실측(§7): 실사용 56% (6회차 33%↑), 구조적 결함 0건, 유형 분포 설정값 수렴.

## 세션 4 — 검증기 캡슐화 + 연구 기법 (08-05 오후, 로컬)

| 커밋 | 내용 |
| --- | --- |
| `83af3bc` | 기계 검사에 문장 번호(sN) 유출 차단 — 7회차 "한 조각 전멸" 패턴 대응. 한글은 `\b`가 안 먹혀 룩비하인드 `(?<![0-9A-Za-z])s\d+` 사용 |
| `32ddf7e` | **검증 전체를 `app/core/quality/`로 캡슐화** — 입력 계약이 "문항+근거텍스트+LLM"뿐이라 학습 페이지(JIT·진단·형성평가)도 같은 `validate_items()` 재사용. 연구 기법 3종 도입: ① 풀이 왕복 검증(정답 가리고 풀이자 LLM이 풀게 → 복수 정답 실험 검출) ② 불합격 수정 1회(폐기 대신 사유 첨부 재작성) ③ 오답 선지 과생성+선별(distractorPool → 코드가 3개 선별). 테스트 55개 |
| `0339294` | 8회차 실측 기록(§9): **실사용 74%**, sN 유출 0건. 남은 결함 7건 = 근거 오매칭 4 + cloze 구절 통째 빈칸 3 |
| `05b6534` | EXAONE 교차 모델 검증 지원 — 생성(Solar)과 다른 모델(EXAONE)로 심판·풀이. `core/llm/exaone.py` (OpenAI 호환, friendli.ai) |

## 세션 5 — EXAONE 연결 실측 확인 (08-05 저녁, 데스크톱)

| 커밋 | 내용 |
| --- | --- |
| `769dae3` | EXAONE 기본 모델 `K-EXAONE-236B-A23B`로 변경. **friendli.ai 실호출로 연결 확인 완료** (키 인식·응답 정상) — 교차 검증 체계(생성 Solar / 심판·풀이 EXAONE) 가동 가능 상태 |

## 세션 6 — 교차 검증 실측 → 배심원단 (08-06, 데스크톱)

**한 일:** Solar 단독 vs EXAONE 교차 A/B 실측 → EXAONE 단독 심판은 3회 연속 측정 불가
(형식 붕괴) → 원인 3종 해결(추론 끄기 / serverless→**dedicated 엔드포인트** 전환으로
분당 3회 제한·2,000토큰 캡 소멸 / 파서 구제) → **배심원단 설계·구현**(심판·풀이를
Solar+EXAONE 둘 다, 한쪽이 잡으면 탈락, 2차 판독 불가는 기권) → 10회차 실측
**저장 22 / 폐기 19 / 파싱 실패 0**, EXAONE 실질 기여 16건. 상세는 QUIZ_TUNING §10.

| 커밋 | 내용 |
| --- | --- |
| `e975394` | K-EXAONE 실동작 1차 수정 — enable_thinking=false·스로틀·잘린 JSON 구제 |
| (커밋 대기) | 배심원단(validator second_llm)·neutral_example 프롬프트·index 폴백·dedicated 배선 — 테스트 62개 통과 |

주의: `.env`에 `EXAONE_BASE_URL=https://api.friendli.ai/dedicated/v1`,
`EXAONE_MODEL=<Endpoint ID>` 필요 (경진대회 키는 dedicated 전용, serverless로 쓰면
분당 3회 제한에 걸림). 이후 **커밋·푸시는 사용자 지시가 있을 때만.**

## 세션 7 — 문제집 프론트 (08-06, 데스크톱)

**한 일:** 확정안 §6 문제 페이지 프론트 구현 (전부 mock 기반 — 백엔드 응답 스키마와 1:1).
흐름: **과목 선택(책장 스타일 책 카드) → 범위 선택 → 풀이 → 결과** 4단계.

- 신규: `pages/QuizPage.tsx` + `pages/quiz/{CourseSelect,ScopeSelect,SolveView,ResultView,mock}.tsx`
- 팀원 파일 수정 2개(각 2줄): `routes.tsx`(/quiz), `AppLayout.tsx`(사이드바 "문제집")
- 스타일은 팀원 문법 그대로: 책장 커버 팔레트·해시 배정 재사용(같은 코스 = 같은 표지색),
  BookCard·ContinueBanner·정오색(#10b981/#ef4444)·radius 규칙 준수
- 기존 학습 블록(McqBlock 등)은 **재사용 안 함** — 정답이 클라에 있는 구조라 서버 채점
  계약과 상충. quiz 전용 컴포넌트로 만들되 시각 문법만 동일하게
- mock은 서버 역할 시뮬레이션: 정답은 비공개 영역, 세션 응답에선 제거, 채점 규칙은
  백엔드 grading과 동일 (교체 = fetch 함수만)
- **UX 추가**: 최근 학습 과목 추천 배너 + 목차 "📖 학습함" 라벨 — 격리 원칙 선긋기는
  QUIZ.md §3.5 참조. 백엔드 필드(last_activity_at·studied)는 **나중에** (§3.5 TODO —
  원본 테이블이 아직 DB에 없어 지금 구현 불가)
- **기출 스타일 별도 모드** (QUIZ.md §3.6): 기출 올린 과목만 [기출 스타일로 풀기] 버튼
  노출 → 실기 단답형 말투의 mock 세트로 풀이 흐름 동작. 백엔드(style 컬럼·2차 패스
  생성·has_exam_style 플래그)는 기출 프로파일 추출(③) 이후
- 실행: `cd frontend && npx pnpm@9 dev` (mock이라 도커·백엔드 불필요). tsc strict 통과
- 커밋 대기 (사용자 지시 필요)

---

## 세션 8 — 실데이터 스모크(11회차) → 품질 개선 3종 (08-06, 데스크톱)

**한 일:** 팀원 파싱 결과(fixtures/parsed_sample.json)로 배심원단 스모크 11회차
(`--chunks 3 --budget 20 --verifier jury`, 저장 12/폐기 29) → 분석에서 나온
개선 거리 3개를 코드로 반영 (상세: QUIZ_TUNING §11). **테스트 68개 통과.**

1. **채점 정규화** — `core/quality/checks.py::check_solution`: 풀이자가 답을
   `["로킹 단위"]`처럼 1원소 배열로 감싸면 정답인데 폐기되던 것을 unwrap
   (shortAnswer·trueFalse만 — cloze/mcq는 배열에 의미가 있어 제외)
2. **근거 오매칭 차단 (§9-① 시행)** — `selection.py` 정의 키워드 교차 점수 +
   행 시작 가점 + 관련도 정렬 / `generation.py::evidence_scope_reason` +
   `service.py`: evidence가 선별 후보 집합 밖이면 폐기 (이름 변형 시 합집합 완화)
3. **경어체 기계 검사** — `checks.py`에 습니다/ㅂ니다·해요체 어미 정규식
   ("아니다" 오탐 방지 위해 통짜 '니다' 매칭 금지). 수정 루프로 회생 가능

- 신규 테스트 6개 (unwrap 1 · 경어체 2 · scope 3)
- **12회차 스모크로 실측 완료** (QUIZ_TUNING §11.1): 저장 11/폐기 29.
  scope 차단 6건 실동작 → 저장분 근거 오매칭 소멸 · 배열 감싸기 폐기 0건 ·
  경어체 오탐 0. 새 표본: 나선형 cloze 빈칸 4개 → §9-②에 "빈칸 ≤2 제한" 추가 예정
- **프론트: 문항 수 선택** — ScopeSelect에 10/20/30/직접 입력 4옵션 (기존 10 고정 제거).
  범위 초과 요청은 있는 만큼으로 클램프(버튼 라벨이 실제 개수 표시). 형태별 유형
  배분(생성 시점)과 무관 — 서빙 샘플링 개수일 뿐. **남은 개선**: 서빙 층화 샘플링
  (목차 비례·개념 중복 회피·유형 라운드로빈 — `repository.py::sample_items`가 현재
  순수 랜덤이라 count가 작을수록 유형 쏠림 가능)
- 커밋 대기 (사용자 지시 필요)

---

## 세션 9 — integration 확인 + pro3 전환·체질 개선 (08-07, 데스크톱, integration 브랜치)

**한 일:** ① 팀원 integration 브랜치 검증 (quiz-create 전체 포함 확인, 테스트 251개
로컬 재현) ② **서비스 기준 모델 = solar-pro3 확정 (pro2 미사용, 사용자)** —
`QUIZ_CHAT_MODEL`을 pro3로 바꾸고 13~16회차 실측하며 파이프라인을 pro3 체질로
재편. 상세는 QUIZ_TUNING §12 (이 세션의 정본 기록).

- 1차: 파서 이어붙임/단일 객체 구제·문체/sN 프롬프트 강화·채점 정규화 (13·14회차)
- 2차 재편: **json_object 래퍼 계약**(생성·심판·풀이·수정 전 콜)·풀이자 전 문항
  응답 강제·**sN 인용 코드 스크럽**(`scrub_sentence_refs`)·단답 콜론 관용·
  **§9-② 시행**(cloze 빈칸≤2·15자·화살표 — §9 숙제 완료) (15·16회차)
- 3차 **pro3 네이티브 재구성** (§12.5, 사용자 가설 "파이프라인이 pro2 기반이라
  그런 것"에 따라): ① cloze를 코드가 조립(`build_cloze_segments` — LLM은 문장·
  용어만 선택, 수정 루프 제외) ② 풀이 대조 관용(괄호 병기·접미 수식·"2번" 표기)
  ③ 생성 프롬프트 백지 재작성(금지 12개 → 계약 7개 + few-shot 예시)
- 실측: pro2 기준 11 → pro3 초기 2 → ①② 후 8 (18회차) → ③ 후 **21** (19회차,
  **pro2 초과**). few-shot 기여가 최대. 저장분 스팟 체크 양호, 유형 배분 정상
- 테스트 263개 통과. **integration 브랜치에서 작업, 커밋 대기 (사용자 지시 필요)**
- **실데이터 엔드투엔드 검증 완료** (§12.7): 정처기 필기 PDF → 도커+DB로 업로드→
  파싱(75초)→생성(888초, 배심원단)→**저장 158/폐기 200**→세션·채점 왕복 정상.
  같은 경로 pro2 기준(INTEGRATION.md)은 저장 3 — 문제은행이 실제로 쓸 만한
  규모가 됨. 잔여: 선별 "근거 못 찾음" 27건·서빙 층화 샘플링·cloze 수율(§12.6~7)
- **생성 API 비동기화 완료**: `POST /quiz/from-parsing`이 **202 접수** 후
  `GET …/status` 폴링 (idle|running|done|failed, 중복 접수 409). 생성 888초를
  동기로 붙잡던 타임아웃 위험 해소 — 업로드→파싱 폴링과 같은 사용법. 잡 상태는
  인메모리(`quiz/jobs.py` — 프로세스 재시작이면 생성 자체가 죽으므로 DB 불필요,
  워커 늘리면 이관). 도커 실검증: 접수 2초·409·done 도달·기존 은행 무손상.
  INTEGRATION.md API 표 갱신. 테스트 265개 통과

---

# 현재 상태 (08-06 기준)

- ✅ 백엔드 파이프라인 + 검증 배터리(core/quality) — 완성, 테스트 68개 통과
- ✅ 문항 품질: 실사용 가능 비율 25%(1회차) → 74%(8회차) → **배심원단 도입** (10회차: 파싱 실패 0, EXAONE 기여 16건)
- ✅ EXAONE dedicated 엔드포인트 연결 (분당 제한·토큰 캡 해소)
- ⬜ 배심원단 코드 커밋 대기 (사용자 지시 필요) — 아래 "다음 작업" 참고

# 다음 작업 (우선순위 순)

0. **커밋** (지시 대기) — 배심원단 백엔드 + 문제집 프론트 + 문서
0-1. **quiz 요약 API에 추천 필드 자리 예약** — `last_activity_at`·`studied` 기본값 반환
     (QUIZ.md §3.5 TODO ①). 실제 집계는 학습 테이블 마이그레이션 후 (§3.5 TODO ②)
1. **QUIZ_TUNING §9 마무리** — ① 선별 개선(근거 오매칭)은 **완료** (세션 8, §11):
   - ~~선별: 정의 키워드 교차 점수 + 표 행 시작(주어) 가점, evidence ⊆ 계획 후보 차단~~ ✅
   - 기계 검사: 빈칸 answer 공백제거 15자 초과 폐기 + 화살표(→/▶) 포함 폐기 + **빈칸 수 ≤2 제한**
     (12회차 저장 [2] 나선형 cloze가 빈칸 4개 — 살아있는 표본) — 남음
   - ①은 12회차로 실측 완료 (§11.1)
2. **프론트 `/quiz` 페이지** — 범위 선택/풀이/결과 3화면, 기존 BlockRenderer 재사용. 주의: 기존 블록은 정답이 클라에 있다는 전제라 서버 채점 모드 필요
3. **배치 비동기화** — 생성 API가 현재 동기(실데이터 몇 분 → 타임아웃). BackgroundTasks + gen_status 폴링으로
4. **기출 스타일 프로파일 추출** — 파이프라인에 꽂는 자리(`exam_frequency`, `stem_patterns`)는 이미 있음. 팀원 기출 파싱 형식 확정 대기
5. **팀원 JSON 계약 대조** — 샘플 오면 QUIZ_INPUT.md와 대조, 다르면 어댑터 1개

# 주의사항 (사고 이력)

- `backend/.env`는 **UTF-8 필수** — PowerShell `>` 리다이렉트는 UTF-16으로 저장해 dotenv가 죽음. `Out-File -Encoding utf8` 사용
- `.env`는 절대 커밋 금지 (UPSTAGE_API_KEY·EXAONE_API_KEY 들어 있음)
- 푸시는 `feat/quiz-create`로
- 품질 판단은 폐기율이 아니라 **"저장분 중 실사용 가능 비율"** · 표본은 `--chunks 3 --budget 20` 이상으로
