# metalearn — 지금 실제로 구현된 것 & 정해야 할 방향

> 회의 때 정한 방향과 지금 코드에 실제로 들어간 것 사이의 간극을 맞추기 위한 문서.
> 프론트/백 같이 보고 다음 방향을 정하기 위함.

- **브랜치**: `feat/yoonhs-integration`
- **기준**: v2 + 배치고사 UI + 다중 PDF 통합
- **상태**: 실동작 확인됨 (4-PDF E2E 통과)

---

## 1. 지금 실제로 도는 흐름

**① 다중 PDF 업로드 → 1개 코스로 통합**
여러 PDF를 파일명 숫자로 자동 정렬 + 순서 확정 + 코스 이름 입력. 주교재(순서 있음) / 보조자료(RAG 근거) 구분해서 **하나의 코스**로 이어붙임.

**② ingest 파이프라인 → 커리큘럼 뼈대 생성**
파싱(Solar) → 정제 → 청킹 → 임베딩 → 개념 추출 → 개념 그래프 → dedup → **커리큘럼 트리(챕터·절)**. 진단 *전에* 커리큘럼 구조가 이미 만들어짐.

**③ 배치고사 = 최소화된 진단**
천장에서 선수 사슬을 타고 하강, 5~12문항으로 `floor`(시작점)·`ceiling`(목표점)만 찾음. 문항별 정오답 피드백 없이 빠르게. 답변은 mastery(BKT)로 누적.
> ⚠️ 이건 "수준" 진단 — 회의에서 말한 "성향" 진단이 아님.

**④ 결과 → 커리큘럼 개인화**
floor/ceiling 기준으로 모든 개념을 `mastered`(건너뜀) / `todo`(배울 것) / `locked`(잠금)로 분류해 `concept_mastery`에 저장. 약한 선수개념은 **[선행] 장**으로 본편 앞에 삽입.

**⑤ 학습 시작 (JIT 생성)**
floor 위치부터. 각 절은 봉투 블록(개념·비유·빈칸·객관식·설명하기)을 그때그때 Solar로 생성 — **설명 먼저 → 인출 나중**(최근 보강).

**⑥ 인출학습 = 평가 (핵심)**
빈칸/객관식/설명하기를 풀면 → `attempts` 누적 + BKT로 mastery 갱신 → 잠금 해제·선행 삽입·복귀 유도. **재진단 없음** — 학습 자체가 평가.

**⑦ 데이터 누적**
`attempts`(답변 로그) + `concept_mastery`(강도·상태)가 계속 쌓임.

---

## 2. 회의 결정 vs 실제 구현

| 회의에서 정한 것 | 상태 | 실제 구현 |
|---|---|---|
| 진단 = 성향(MBTI식) + 가벼운 기반지식 체크 | 🟡 부분 | 기반지식(floor)만 있음. **성향은 코드에 전혀 없음** |
| 학습 내용 줄이지 않고 풀 컨텍스트로 생성 | 🟢 구현됨 | 절 블록 풀 컨텍스트 생성 O |
| 성향 기반 스타일(비유·예시) 맞춤 생성 | 🔴 없음 | 생성 입력에 성향 항목 자체가 없음 |
| 개념 그래프에 선수지식 추가 | 🟡 부분 | 선수개념·[선행] 장은 있음. 진단 연동은 약함 |
| 재진단 없이 인출학습을 평가 신호로 | 🟢 구현됨 | attempts → BKT mastery 갱신 O |
| 다음 장 생성에 오답노트 + 복습 weaving | 🔴 없음 | 앞 장 인출 오답이 다음 장 생성에 안 들어감 |
| 오답노트 누적 | 🔴 없음 | attempts는 쌓이나 생성에 반영 안 됨 |

---

## 3. 방향과 어긋나는 두 지점

**1. 지금은 "수준으로 커리큘럼을 조정"한다.**
floor/ceiling → mastered(건너뜀)·locked(잠금)은, 회의에서 *지양하자*고 한 "진단 → 수준평가 → 커리큘럼 축소" 구조에 오히려 가깝다. 회의 결정은 "수준으로 줄이지 말고 풀 컨텍스트 + 성향 맞춤"이었다.

**2. 진단을 끝내야만 커리큘럼이 뜬다.**
배치고사 미완료(ceiling 없음)면 트리가 `400`. 즉 커리큘럼이 진단에 강하게 묶여 있어, 회의가 벗어나려던 결합이 그대로 남아 있다.

---

## 4. 정해야 할 방향

**① 커리큘럼 ↔ 진단 분리 (추천 · 먼저)**
배치고사 없이도 풀 컨텍스트 커리큘럼이 바로 뜨게. 진단은 나중에 얹는 레이어로.
→ 지금의 400 버그도 해소 + 회의 방향("진단 최소화")과 정합.

**② 성향 진단 추가 (회의 핵심)**
짧은 성향(MBTI식) 문항 → 생성 스타일(비유·예시·설명 깊이)에 반영.
→ 지금 "수준 진단"을 "성향 진단"으로 전환/보완.

**③ 오답노트 · 복습 weaving (회의 핵심)**
앞 장 인출 오답을 다음 장 생성 프롬프트에 주입 → 복습 + 맞춤.
→ "복습하며 수준 반영" 흐름을 실제로 완성.

---

## 한 줄 요약

- **만든 것**: 다중 PDF 통합 · **수준** 배치고사 · 인출=평가 · 풀 컨텍스트 생성
- **회의 방향인데 안 만든 것**: **성향 진단** · 성향 스타일 맞춤 · **오답노트 weaving**

---

## 5. 다음 회의 안건 (정해야 할 것)

> ✅ **후기(2026-07-10): 아래 논점 4개는 결정·구현됨.** 답의 근거는 `docs/07_09 진단+성향 파이프라인.md`(결정문), 구현은 소민섭 `redesign/diagnostic-profiling` 병합으로 반영.
> - **논점 1(성향)** → 넣음 — 온보딩 disposition 4문항, 3축(representation·rigor·context) EMA, `learner_profiles`, 생성 지시문에만 반영 ✅
> - **논점 2(타이밍)** → 양자택일 아닌 역할 분리 — 실시간=게이트·선행 삽입 / 복습 시점=오답노트 weaving(다음 장 복습 섹션) ✅
> - **논점 3(줄이기)** → 폐지 — 전 절 todo 시딩, mastered/locked 확정 라벨 제거 ✅
> - **논점 4(분리)** → 분리 — diag_status 서버 진실, 진단 없이도 커리큘럼 생성 ✅
> - **잔여 논점 1개**: 게이트 철학 — 구현 「풀면 진행(시도=완료)」 vs 결정문 「2연속 통과=빠른 완료」 → 팀 확정 필요. (+갭 기반 출발점 추천 여부)
>
> 이하는 논점 제기 당시의 원문(역사 기록).

> 구현이 회의 방향과 엇갈려서(특히 "수준 반영 타이밍"), 방향을 다시 맞추기 위한 논점.

### 논점 1 — 성향 진단, 넣나? *(회의 방향인데 구현 0%)*
- 넣는다면: 축(예: 비유형 / 암기형 / 근본이해형 …) · 문항 몇 개 · 생성의 무엇에 반영(비유·예시·설명 깊이)?
- 사실상 회의 방향의 **출발점**인데 지금 완전히 비어 있음.

### 논점 2 — 수준 반영 "타이밍" *(정면충돌 지점)*
- **회의**: 복습 시점 반영 (인출 쌓아뒀다가 다음 장 오답노트로)
- **구현**: 실시간 반영 (매 답변 BKT 갱신 + 실시간 선행 유도)
- → **실시간 / 복습 시점 / 둘 다** 중 택. ("구현이 반대로 갔다"의 핵심)

### 논점 3 — 수준으로 커리큘럼 "줄이기", 유지?
- **회의**: "줄이지 말고 풀 컨텍스트" (지양)
- **구현**: floor 아래 mastered(건너뜀) / ceiling 위 locked(잠금) = 줄이는 중
- → 유지 vs 폐지(다 풀 컨텍스트로).

### 논점 4 — 커리큘럼 ↔ 진단 분리?
- 지금은 진단 끝나야 커리큘럼 뜸(`400`). 회의는 "진단 최소화".
- → 커리큘럼 먼저 뜨고 진단은 얹는 레이어로 분리할지.

### 안 정해도 되는 것 (이미 방향 일치)
풀 컨텍스트 생성 · 인출=평가 · 다중 PDF 통합 → 논쟁 불필요, 그대로 유지.

### 정리
- **논점 1(성향)** 과 **논점 2(타이밍)** 가 진짜 결정. 3·4는 2가 정해지면 따라옴.
  - 복습 시점 반영 → mastered/locked 줄이기 폐지 + 진단 분리 = **회의 원안 복귀**
  - 실시간 반영 → 지금 구현 유지·정교화
